<h1>Listing google_kmls</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @google_kmls.each do |google_kml| %>
      <tr>
        <td><%= link_to 'Show', google_kml %></td>
        <td><%= link_to 'Edit', edit_google_kml_path(google_kml) %></td>
        <td><%= link_to 'Destroy', google_kml, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Google kml', new_google_kml_path %>
